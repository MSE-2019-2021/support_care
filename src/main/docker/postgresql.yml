version: '2'
services:
  supportcare-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/supportcare/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=supportcare
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
